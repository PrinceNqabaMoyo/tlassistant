# Where api/url occur in front end

## 1 

Splasscreen: On portrait phone screens, the 'Acurriculum aligned teaching and learning assistant' is now not spilling over, but it looks like its using a non afacad font



## 2 

&#x20;splash screen: the grad cap and its 4 blocks over lap with fundile text in portrait mobile screed



## 3  the error message seems apis are failing

local host works but once deployed, it doesn't reach hugging face and the error message spills beyond screen in portrait mobile screen it doesn't stay within the answer card text area.: Failed to fetch (http://localhost:5001/api/accounting/grade11/accounting/generate). Check backend is running and VITE\_API\_BASE\_URL / runtime-config.js points to it.

## 

